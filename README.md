# 📸 دانلودر پروفایل اینستاگرام (Tkinter)

یک برنامه دسکتاپ ساده ساخته شده با پایتون و Tkinter که به کاربران امکان می‌دهد تصویر پروفایل یک حساب اینستاگرام را با وارد کردن نام کاربری مشاهده کنند.

---

## 🧩 این پروژه چه کاری انجام می‌دهد؟

این برنامه به شما اجازه می‌دهد:

* وارد کردن نام کاربری اینستاگرام
* نمایش تصویر داخل یک پنجره گرافیکی

و خطاهای زیر را مدیریت می‌کند:

* نام کاربری خالی
* نام کاربری نامعتبر
* مشکلات اتصال به اینترنت

⚠️ این پروژه فقط تصاویر پروفایل عمومی را نشان میدهد و نیازی به ورود به حساب اینستاگرام ندارد.

---

## 🛠 فناوری‌ها و کتابخانه‌های استفاده شده

* Python 3
* Tkinter – رابط گرافیکی (GUI)
* Instaloader – دسترسی به داده‌های عمومی اینستاگرام
* Pillow (PIL) – پردازش و تغییر اندازه تصویر
* urllib – دانلود داده‌های تصویر

---

## 🖥 پیش‌نمایش برنامه (Workflow)

1. کاربر نام کاربری اینستاگرام را وارد می‌کند
3. برنامه تصویر پروفایل را از اینستاگرام دریافت می‌کند
4. تصویر تغییر اندازه داده شده و داخل پنجره برنامه نمایش داده می‌شود

---

## 📂 ساختار پروژه

```
Instagram Project/
│
├── Instagram Project.py     # فایل اصلی برنامه
├── requirements.txt         # کتابخانه‌های مورد نیاز پایتون
├── README.md                # مستندات پروژه
└── .gitignore               # فایل‌ها و پوشه‌های نادیده گرفته شده
```

---

## ⚙️ نصب و راه‌اندازی

### 1️⃣ کلون کردن مخزن

```bash
git clone https://github.com/your-username/your-repo-name.git
cd your-repo-name
```

### 2️⃣ ایجاد محیط مجازی

```bash
python -m venv .venv
```

فعال‌سازی محیط مجازی:

**Windows**

```bash
.venv\Scripts\activate
```

**Linux / macOS**

```bash
source .venv/bin/activate
```

### 3️⃣ نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

### 4️⃣ اجرای برنامه

```bash
python "Instagram Project.py"
```

---

## 📦 مثال محتویات requirements.txt

```
instaloader
pillow
```

(Tkinter به‌صورت پیش‌فرض با پایتون نصب می‌شود)

---

## ⚠️ نکات مهم

* فقط با پروفایل‌های عمومی اینستاگرام کار می‌کند
* نیاز به اتصال اینترنت فعال دارد
* محدودیت‌های نرخ اینستاگرام ممکن است اعمال شود
* این پروژه صرفاً برای اهداف آموزشی است

---

## 🧠 نحوه عملکرد کد (توضیح سطح بالا)

* **InstagramProfileDownloader**
  → کلاس اصلی که پنجره GUI را ایجاد می‌کند

* **create_widgets()**
  → ایجاد فیلد ورودی، دکمه و بخش نمایش تصویر

* **get_profile_image()**
  → اعتبارسنجی نام کاربری و مدیریت جریان برنامه

* **download_image()**
  → دانلود تصویر پروفایل با استفاده از Instaloader

* **show_image()**
  → تغییر اندازه و نمایش تصویر با Pillow

---

## 🚀 بهبودهای آینده

* ذخیره تصویر در حافظه محلی
* پشتیبانی از اندازه‌های مختلف تصویر
* افزودن انیمیشن بارگذاری
* رابط کاربری حالت تاریک (Dark Mode)
* پشتیبانی از پروفایل‌های خصوصی با ورود به حساب

---

## 📜 مجوز

این پروژه متن‌باز است و استفاده از آن برای اهداف آموزشی و یادگیری رایگان است.

---

## ⭐ نکته نهایی

اگر این پروژه را دوست داشتید، به آن در GitHub یک ⭐ بدهید!
